#ifndef UTILS_H
#define UTILS_H

/*=========
* METHODS: *
==========*/

/* Utility helpers (stub) */

void log_msg(const char *fmt, ...);

/**
 * strupr for linux
 * @param buffer The string we want to put in upper characters.
 */
void strupper(char *buffer);

#endif /* UTILS_H */
